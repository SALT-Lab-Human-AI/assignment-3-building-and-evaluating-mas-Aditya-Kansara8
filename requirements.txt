autogen-agentchat
autogen-ext[groq]

langgraph
langchain
langchain-core
langchain-community

groq
openai  # Required for AutoGen's OpenAIChatCompletionClient (Groq uses OpenAI-compatible API)

guardrails-ai
nemoguardrails

tavily-python
requests
beautifulsoup4
aiohttp

semanticscholar
sentence-transformers  # For embedding-based relevance scoring and deduplication

streamlit
gradio
flask
fastapi
uvicorn

python-dotenv
pydantic
pyyaml

pytest
black

# Pre-commit hooks for security and code quality
pre-commit
detect-secrets
# NOTE: gitleaks is NOT a Python package - it's a Go binary tool
# Install separately:
#   macOS: brew install gitleaks
#   Linux: See https://github.com/gitleaks/gitleaks#installation
#   Or download from: https://github.com/gitleaks/gitleaks/releases
bandit